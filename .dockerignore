.git
.venv
venv
env
__pycache__
*.pyc
.pytest_cache
.ruff_cache
.DS_Store

datasets
data
outputs
runs
reports
notebooks

*.zip
*.mp4
*.avi
*.mov
*.mkv

# Model weights are ignored by Git (see .gitignore) but are intentionally
# included in the Docker build context for local packaging. Do NOT add
# models/ or *.pt here — best.pt must be copied into /app/models/ at build time.
